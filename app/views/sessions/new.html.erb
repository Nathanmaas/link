  <h1>Login</h1>

  <%= render "shared/flash" %>

  <%= form_for :user do |f| %>
    <%= f.text_field :email, placeholder: "email" %>
    <%= f.password_field :password, placeholder: "password"%>
    <%= f.submit "Login", class: "btn btn-success glyphicon glyphicon-sm"%>
  <% end %>

  Join Our Community - Become a Member
  <%= link_to signup_path %>